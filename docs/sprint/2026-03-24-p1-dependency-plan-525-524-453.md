# Sprint Dependency Plan (2026-03-24)

## Scope
Pending issues analyzed:
- #525
- #524
- #453

## Signal Report

### Issue #525 — Sprint orchestrator drops source field
- Status on `origin/main`: **open / reproducible**
- Root cause: `run_repo_sprint` builds `CreateTaskRequest` without `source`, causing completion callback in `http.rs` to return early when `task.source` is missing.
- Primary touch files:
  - `crates/harness-server/src/intake/mod.rs`
  - `crates/harness-server/src/http.rs` (callback behavior context)
- Coupling notes: Does not overlap with #524 or #453 file ownership.

### Issue #524 — periodic_reviewer lock pattern flagged as nested/deadlock risk
- Status on `origin/main`: **open / reproducible**
- Root cause: sequential acquisition pattern over `fallback_ts` and `poll_handle` in `run_review_tick` is ambiguous and guard-flagged as RS-01 risk.
- Primary touch files:
  - `crates/harness-server/src/periodic_reviewer.rs`
- Coupling notes: No file overlap with #525 or #453.

### Issue #453 — read lock held during async scan
- Status on `origin/main`: **open / reproducible**
- Root cause: `state.engines.rules.read().await` guard is held while awaiting `scan_files(...).await` / `scan(...).await`, blocking write lock acquisition in `rule_load`.
- Primary touch files:
  - `crates/harness-server/src/handlers/rules.rs`
- Coupling notes: No file overlap with #525 or #524.

## Dependency Decision
All three issues are independent by code ownership and execution path. No strict prerequisite among pending issues is required.

SPRINT_PLAN_START
{
  "tasks": [
    {"issue": 525, "depends_on": []},
    {"issue": 524, "depends_on": []},
    {"issue": 453, "depends_on": []}
  ],
  "skip": []
}
SPRINT_PLAN_END
