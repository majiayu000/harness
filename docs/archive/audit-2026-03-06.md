# Harness Design Audit — 2026-03-06

## Summary

32 items audited. 10 already fixed, 2 re-evaluated (1 downgraded, 1 confirmed fixed). **13 items remain.**

## Remaining Issues

### Critical (2)

| # | File | Issue | Fix |
|---|------|-------|-----|
| 1 | `harness-server/src/test_helpers.rs:45` | `ws_token: "test-token".into()` — field does not exist in `AppState`. Compilation failure. | Remove the `ws_token` line from `test_helpers.rs`. |
| 2 | `harness-server/src/handlers/mod.rs` | Dual violation-logging pipeline: `persist_violations()` vs `EventStore::persist_rule_scan()`. The handlers path does NOT write a `rule_scan` anchor event, breaking session-scoped violation counting in `metrics_query` and `http.rs::build_app_state`. | Replace all calls to `handlers::persist_violations()` with `events.persist_rule_scan()`, then delete `persist_violations`. |

### High (4)

| # | File | Issue | Fix |
|---|------|-------|-----|
| 4 | `harness-core/src/config.rs` | Unused config fields: `RulesConfig::discovery_paths`, `RulesConfig::builtin_path`, `ObserveConfig::session_renewal_secs`, `ObserveConfig::log_retention_days` — defined but never read at runtime. | Either wire into startup code or remove from config struct + `default.toml`. |
| 6 | `harness-agents/src/anthropic_api.rs:71` | `max_tokens: 4096` hardcoded in API request body. | Add `max_tokens: u32` to `AnthropicApiConfig` (default 4096), read from config. |
| 8 | `harness-server/src/http.rs:107-109` | `SignalThresholds::default()` always used, ignoring `HarnessConfig::gc.signal_thresholds` from `default.toml`. | Pass `config.gc.signal_thresholds` into `SignalDetector::new()`. |
| 11 | `harness-server/src/thread_manager.rs` | `db: Option<ThreadDb>` field and `open()`, `persist()`, `persist_insert()`, `persist_delete()` methods are dead code. Persistence is handled via `AppState.thread_db`. | Remove `db` field and all associated methods from `ThreadManager`. |

### Medium (5)

| # | File | Issue | Fix |
|---|------|-------|-----|
| 17 | `harness-agents/src/anthropic_api.rs:129,136,141` | `let _ = tx.send(...)` — mpsc send errors silently ignored. If consumer drops receiver, agent output is lost without log. | Replace with `if let Err(e) = tx.send(...).await { tracing::debug!("stream channel closed: {e}"); }` |
| 20 | `handlers/observe.rs:46`, `handlers/gc.rs:24`, `handlers/health.rs:26`, `scheduler.rs:55` | `rules.scan(&project_root).await.unwrap_or_default()` — rule scan errors silently swallowed (4 call sites). | Log via `tracing::error!` before falling back to empty vec, or propagate error to caller. |
| 22 | `harness-core/src/prompts.rs:36` | `check_existing_pr()` hardcodes `/gemini review` bot command and `{owner}/{repo}` template placeholders. Only works with Gemini bot. | Make review bot command configurable (parameter or config), or document the constraint. |
| 24 | `harness-server/src/handlers/learn.rs:248` | `detect_severity` uses keyword matching (`contains("critical")`) that false-positives on words like "critical path", "critical thinking". | Check for explicit `severity: <level>` line first; fall back to word-boundary regex. |
| 25 | `scheduler.rs:55` | Same as #20 — `scan().unwrap_or_default()`. | (Grouped with #20) |

### Low (2)

| # | File | Issue | Fix |
|---|------|-------|-----|
| 29 | `harness-server/src/task_runner.rs` | `waiting_count` TOCTOU: after pushing "waiting" result, immediately queries `store.get()` to count. Unnecessary overhead when a local counter would suffice. | Use a local `u32` counter instead of re-reading from store. |
| 31 | 6 files: `router.rs`, `http.rs`, `stdio.rs`, `websocket.rs`, `scheduler.rs`, `test_helpers.rs` | `make_test_state` duplicated with diverging implementations (different channel capacities, interceptors, fields). | Consolidate: all test modules should use `crate::test_helpers::make_test_state`. |

## Already Fixed (10)

| # | Issue | How Fixed |
|---|-------|-----------|
| 3 | `signal_priority` duplicate | `gc_agent.rs` now uses `crate::remediation::signal_priority` |
| 5 | GC task params hardcoded | `gc_adopt_task_request` reads from `GcConfig`, with tests |
| 7 | `PathBuf::from(".")` hardcoded | `state.project_root` + `resolve_project_root()` from config |
| 10 | `data_dir()` ignores config | Deleted; replaced with `resolve_project_root()` |
| 12 | `discovery.rs`/`matcher.rs` dead modules | Files deleted |
| 16 | notify channel silently drops | `record_drop()` with `tracing::warn!` + counter + tests |
| 19 | Unknown status string silent downgrade | `str_to_status` returns `anyhow::bail!` + test |
| 23 | broadcast channel 256 hardcoded | Now reads `server.notification_broadcast_capacity` from config |
| 28 | `detect_languages()` never called | Function deleted |
| 30 | `SessionManager`/`SessionMetrics` unused | Files deleted |

## Re-evaluated (2)

| # | Original Issue | Verdict |
|---|----------------|---------|
| 9 | `baseline_violations` synthetic | **Downgraded to Low** — actually reads from `latest_baseline_scan()` via EventStore, real data |
| 13 | `unwrap()` on `current_dir()` | **Already fixed** — uses `?` propagation with `with_context()` |

## Harness Self-Repair Tasks

| Task ID | Issues | Status |
|---------|--------|--------|
| `678021f2-204e-4233-ae8d-dc2ef65d2989` | #1 (ws_token) | submitted |
| (pending) | #2 (dual pipeline) | queued |
| (pending) | #4+#8 (config wiring) | queued |
| (pending) | #6 (max_tokens) | queued |
| (pending) | #11 (dead code) | queued |
| (pending) | #17+#20+#25 (silent errors) | queued |
| (pending) | #22 (check_existing_pr) | queued |
| (pending) | #24 (detect_severity) | queued |
| (pending) | #29+#31 (low priority cleanup) | queued |
